@model Restaurant

@{
    ViewBag.Title = Model.Name;
}

<h1>@Model.Name</h1>
<div>Id: @Model.Id</div>
<div>Cuisine: @Model.Cuisine</div>
<div>
    <a asp-action="@nameof(HomeController.Index)">Back to home</a>
</div>